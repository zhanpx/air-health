# 隐私与安全

## 适用范围

此仓库是单用户教学参考，不是经过独立安全审计的生产发行版。请不要直接开放陌生人注册，也不要用于医疗诊断或治疗决策。

## 绝对不要提交

- Google `config.json`、`token.json`、client secret 或完整 OAuth callback URL。
- Codex `.codex/`、`auth.json`、API Key 或其他登录缓存。
- SQLite、WAL、原始记录、GPS、报告、备份和运行日志。
- 真实健康截图、设备 ID、完整设备同步时间、手机号、邮箱或地址。
- `.env`、私钥、证书私钥、网站密码摘要和会话密钥。

`.gitignore` 只是第二道防线：已跟踪文件不受它保护。提交前检查 `git diff --cached` 和 `git ls-files`，用白名单导出代码。发布后删除当前文件不能撤回 Git 历史中的泄漏内容。

## 运行边界

- 后端 8765 和网关 8766 只监听 loopback。
- HTTPS 隧道仅通往登录网关；管理配置与 OAuth 入口不对公网开放。
- 代码 root 管理，数据由专用用户持有，目录 0700、敏感文件 0600。
- Google OAuth、网页登录、AI 身份是三种独立机制。
- AI 默认只收最小摘要，图片必须由用户主动上传；不发送精确 GPS 或凭据。
- MCP 仅开放固定只读工具；工具注释和只读 SQLite 不等于操作系统隔离。
- 备份含健康数据；需要同级访问控制、异地加密和恢复演练。

## 已知需补强部分

- OAuth state 是单个服务端状态，非多用户会话绑定。
- 同步锁是进程内锁；Web 与 systemd 写入入口需统一调度或跨进程锁。
- 网关的单密码摘要和内存限流不等同成熟身份平台；生产环境需完整认证与 CSRF 防护。
- 模型提示词的“不要使用工具”不是强制执行边界。
- 临时图片正常结束会清理，进程崩溃时需要额外清理策略。
- API 数据源、晚到数据、日期与覆盖率需要持续对账。

## 问题报告

普通 bug 可以提交完全脱敏的 Issue。敏感漏洞先使用仓库可用的私密报告方式；如未开启，不要在公开 Issue 中发利用细节、凭据或真实数据。

已经泄漏时，先限制入口并撤销/轮换相关凭据，再处理缓存和历史；不要仅删除文件后继续使用旧令牌。
